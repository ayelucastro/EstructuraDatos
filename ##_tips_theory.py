"""tips
variable.lower() #passes letters to lowercase
variable.upper() #passes letters to uppercase
list_name.append(variable)  #add 1 element at a time at the end
list_name.insert(variable)  #add 1 element at any desired position


ingresar parametro primero  .pop() #removes the last element in a list
ingresar parametro primero  .pop(position) #to remove from an specific position.  If I put .pop(0) repeteadly, the list empties.
ingresar parametro primero  .remove('value')  #removes an specific element from the list
push : [variable].append(pedido_pizza,arepas,etc)
cocina = [list name].pop(0)

###PROGRAMA PRINCIPAL: cerrar asi
if __name__ == '__main__':
	main()


###Concepts:
#centinela: loop, se repite el input adentro y afuera del if/while 
end = "" es para poder escribir en pantalla en la misma línea. 
push an element for it to pop from the queue
the queue is from a stack
FIFO = first in first out
LIFO = last in first out

"""